name: Bug report
description: Report a functional defect in XRayMOD
title: "bug: "
labels: ["bug", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for reporting a bug. **Redact secrets** (tokens, passwords, full SECURE PATH / panel UUIDs, cookies).
        After Gen 5.1.1, bare `/panel` and `/sub` without SECURE PATH return 404 — mention your XRayMOD version.
  - type: textarea
    id: summary
    attributes:
      label: Summary
      description: One-paragraph description of the problem
    validations:
      required: true
  - type: textarea
    id: reproduce
    attributes:
      label: Steps to reproduce
      placeholder: |
        1. Install with …
        2. Open …
        3. Click …
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: Expected behavior
    validations:
      required: true
  - type: textarea
    id: actual
    attributes:
      label: Actual behavior
    validations:
      required: true
  - type: dropdown
    id: component
    attributes:
      label: Component
      options:
        - Installer (shell/ps1)
        - Worker runtime
        - Admin panel UI
        - Admin Dashboard
        - Subscription /me portal (SECURE PATH)
        - Documentation
        - Other
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: Version / commit
      placeholder: v5.1.1 or commit SHA
      description: Prefer `v5.1.1` or newer (SECURE PATH generation)
  - type: dropdown
    id: os
    attributes:
      label: Operator OS
      options:
        - Windows
        - macOS
        - Linux
        - Other
  - type: textarea
    id: logs
    attributes:
      label: Logs (redacted)
      render: text
  - type: checkboxes
    id: checklist
    attributes:
      label: Checklist
      options:
        - label: I searched existing issues
          required: true
        - label: I removed tokens/passwords/secrets from this report
          required: true
